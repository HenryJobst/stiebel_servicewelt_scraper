# CVEs in pip's own vendored copies (pip/_vendor/vendor.txt), not installed
# as importable top-level packages and never used by this application.
# Only fixable by pip itself shipping updated vendored copies.
GHSA-6v7p-g79w-8964
CVE-2025-47273
